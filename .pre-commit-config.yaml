repos:
  - repo: local
    hooks:
      - id: replace-sigma
        name: Replace standalone 'sigma' with 'σ' in docs and docstrings
        entry: python scripts/replace_sigma.py
        language: system
        files: '\\.(py|md|rst|txt)$'
        stages: [pre-commit]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
        args: [--line-length, "100"]

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: [--profile, black, --line-length, "100"]

  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.1.14
    hooks:
      - id: ruff
        args: [--fix, --show-fixes]
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.8.0
    hooks:
      - id: mypy
        # Limit mypy checks to the `src` folder to avoid duplicate-module
        # errors from top-level packages during local runs. Do not pass
        # filenames from pre-commit so the hook only checks the provided path.
        args: [--config-file, pyproject.toml, src, --ignore-missing-imports, --follow-imports=silent]
        pass_filenames: false

  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.8
    hooks:
      - id: bandit
        args: [--config, pyproject.toml]
